import { styled, theme } from "Src/stitches.config";

export const StyledEmailCapture = styled("form", {
  maxWidth: "31.5rem",
  display: "grid",
  gridTemplateColumns: "1fr 8rem",
  gap: "1rem",
});
